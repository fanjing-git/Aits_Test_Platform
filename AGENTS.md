# AITS 项目 Codex 开发规则

本文件是项目根目录规则，适用于 `backend/`、`frontend/`、`scripts/`、`deploy/` 和 `doc/` 下的全部文件。详细规则以以下文档为准：

- `doc/AGENTS.md`：项目会话续接、任务边界和交付流程
- `doc/CONTRACT_ENFORCEMENT.md`：强制门禁和检查点规则
- `doc/测试平台开发核心契约（PythonDjango + 受控Vibe Coding）.md`：完整开发契约
- `doc/SESSION_START_AUDIT.md`：启动审计清单
- `doc/PROJECT_STATUS.md`：当前任务、进度和交接记录
- `doc/TASKS_V5.0.md`：任务清单、依赖和完成标准
- `doc/PRD_V5.0.md`：产品需求
- `doc/TECH_ARCH_V5.0.md`：技术架构

## 必须遵守的硬门禁

1. 每次新会话开始时，先读取状态、任务清单、PRD、技术架构、核心契约、执行补充和启动审计文件。
2. 启动或恢复前后端服务，并检查 `http://127.0.0.1:5173/` 和 `http://127.0.0.1:8000/`；服务失败时先查看 `.runtime/backend.log` 和 `.runtime/frontend.log`。
3. 编码前必须输出完整“会话启动审计结果”，包含服务状态、完成进度、遗漏检查、测试记录、Git 检查点、建议顺序、下一任务和任务边界；等待用户回复“继续”或“确认”。
4. 每次只开发 `TASKS_V5.0.md` 中的一个任务，不能跨任务编码或顺便修改无关模块。
5. 任务必须先完成实现，再执行两轮测试：专项测试，以及全量/集成测试；失败必须修复并重新验证。
6. 每完成一个任务，必须更新 `doc/PROJECT_STATUS.md`，记录完成内容、测试结果、关键决策、下一任务和时间。
7. 完成报告必须列出任务编号、修改文件、新增文件、第一轮测试、第二轮测试、遗留问题和改进建议；报告后停止并等待确认。
8. 业务模块必须按“后端模型/服务 → REST API → 对应前端 → 真实本地 API 联调 → 用户验收”闭环；REST API 完成后不得跳过对应前端闭环。
9. 每完成一个可操作的后端功能，必须立即完成至少一个对应的前端操作入口和真实 API 联调；前端功能未完成、未调试或未形成工作台验收路径时，不得开始下一个后端业务功能。
10. 前端必须覆盖加载、空数据、成功、字段错误、401、403 和服务失败反馈；控制者或决策者必须能从工作台进入并完成核心操作。
11. 前端闭环的严格调试顺序为：页面入口 → API 封装 → 正常操作 → 空数据 → 字段错误 → 401/403 → 服务失败/重试 → 浏览器真实代理联调 → 构建复核；任一关键步骤失败都不能标记完成。
12. 发现历史遗漏、需求或依赖问题、权限或安全问题、交互问题、测试失败或状态记录不一致时，立即报告影响和整改顺序并暂停后续开发。
13. 一次确认五个或以上任务时，编码前执行全历史严格审查；每累计完成五个任务执行兼容性审查、专项测试、全量回归、迁移检查、前端构建、关键端到端验证、Git 提交和附注标签。
14. 未收到明确“推送 Git”指令，不得推送远程仓库；任何回滚必须取得明确授权。

## 实现约束

- 前端使用 JavaScript 和纯 CSS，不使用 TypeScript、SCSS 或 Less。
- 后端公共函数和方法使用类型提示和文档字符串；业务逻辑放在 Service 层，API 使用 DRF Serializer。
- 使用 Django ORM；外部调用必须捕获错误并安全降级。
- 禁止硬编码密钥、明文密码、日志泄露敏感信息、`eval()` 和 `exec()`。
- 配置、依赖、环境变量、数据库迁移发生变化时，必须在完成报告和项目状态中说明。

如果本文件与更深层目录中的规则冲突，以更深层 `AGENTS.md` 或 `AGENTS.override.md` 为准；如果与系统、开发者或用户当前明确指令冲突，以更高优先级指令为准。
