# AI智能体测试平台 PRD 需求文档

## 最终版 V5.0（完整定稿版）

> 本文档整合V4.0全部原有需求 + 六轮补充 + 最终三轮补充
> 本文档为开发唯一需求真理源，完整独立，不引用任何历史版本

---

## 文档信息

| 项目         | 内容                                                         |
| ------------ | ------------------------------------------------------------ |
| **产品名称** | AI智能体测试平台                                             |
| **文档版本** | V5.0（最终定稿）                                             |
| **文档状态** | 已定稿                                                       |
| **目标用户** | 开发工程师、测试工程师、测试负责人、系统管理员               |
| **产品定位** | 对话驱动的AI智能体测试平台，覆盖需求分析→用例生成→测试执行→报告分析全链路 |

**🟧【V5.1 已人工验收】以下带“🟧”的内容为本次新增或调整，现已纳入正式产品范围。**


## 目录

1. [产品概述](#1-产品概述)
2. [用户与场景](#2-用户与场景)
3. [核心架构](#3-核心架构)
4. [功能模块总览](#4-功能模块总览)
5. [F1 对话交互系统](#5-f1-对话交互系统)
6. [F2 认知决策系统](#6-f2-认知决策系统)
7. [F3 任务管理系统](#7-f3-任务管理系统)
8. [F4 测试能力系统](#8-f4-测试能力系统)
9. [F5 测试环境管理](#9-f5-测试环境管理)
10. [F6 知识库系统](#10-f6-知识库系统)
11. [F7 Skills技能系统](#11-f7-skills技能系统)
12. [F8 智能检索系统](#12-f8-智能检索系统)
13. [F9 模型管理系统](#13-f9-模型管理系统)
14. [F10 提示词管理系统](#14-f10-提示词管理系统)
15. [F11 主动测试建议](#15-f11-主动测试建议)
16. [F12 Git集成与自动触发](#16-f12-git集成与自动触发)
17. [F13 通知中心](#17-f13-通知中心)
18. [F14 报告系统](#18-f14-报告系统)
19. [F15 测试资产沉淀与复用](#19-f15-测试资产沉淀与复用)
20. [F16 测试效果度量与ROI](#20-f16-测试效果度量与roi)
21. [F17 协作与权限系统](#21-f17-协作与权限系统)
22. [F18 安全与治理系统](#22-f18-安全与治理系统)
23. [开发路线图](#23-开发路线图)
24. [验收标准](#24-验收标准)


## 1. 产品概述

### 1.1 产品定义

AI智能体测试平台是一个**对话驱动的智能测试助手**。用户通过自然语言描述测试需求，平台智能体自主完成需求分析、用例生成、测试执行、结果分析、报告推送全流程。

### 1.2 核心差异化

- **对话即测试**：自然语言交互，无需编写测试代码
- **需求深度分析**：上传/导入需求文档，LLM深度理解并拆解功能
- **联合功能识别**：自动识别跨模块联动场景（功能A在功能C页面展示）
- **用例智能生成**：多轮对比确认，自动评审，自动筛选自动化用例
- **业务联调测试**：不仅测单接口，自动识别并测试业务链路
- **多类型测试**：接口/UI/APP/性能/AI/安全/回归/数据，共9种
- **多模型兼容**：OpenAI/Claude/Gemini/通义千问/DeepSeek等
- **四级提示词**：即时>场景>项目>全局，默认预置
- **知识持续沉淀**：含人工审核，使用越多越智能
- **主动测试建议**：Git变更/质量趋势/环境异常主动通知
- **多渠道通知**：企业微信/钉钉/邮件/平台内消息
- **测试资产复用**：用例模板、策略模板、缺陷模式跨项目复用
- **效果可度量**：ROI仪表盘、时间节省量化


## 2. 用户与场景

### 2.1 用户角色

| 角色       | 特征             | 核心诉求                  |
| ---------- | ---------------- | ------------------------- |
| 开发工程师 | 写完代码快速验证 | 快、简洁、即时反馈        |
| 测试工程师 | 系统化测试       | 全面、详细、高效生成      |
| 测试负责人 | 质量管控         | 数据、趋势、团队协作、ROI |
| 系统管理员 | 平台维护         | 配置、权限、监控          |

### 2.2 典型场景

#### 场景一：需求文档生成用例
```
测试员：[粘贴腾讯文档链接]
智能体："检测到腾讯文档《订单模块需求V2.1》，正在读取...
✅ 读取成功，识别到8个功能模块，21个功能点，3个联动场景。

接下来我将进行：
1. 需求深度分析
2. 联合功能识别
3. 测试点分析
4. 用例生成（多轮对比确认）
5. 用例评审（多轮对比确认）
6. 自动化用例筛选

预计生成50-60条用例，开始吗？"
```

#### 场景二：业务联调测试
```
测试员："帮我测完整的下单流程"
智能体："识别到业务链路：登录→浏览商品→加购物车→创建订单→支付→订单状态更新

已生成联调用例18条，包含：
- 正常流程用例6条
- 异常分支用例8条
- 边界场景用例4条

执行顺序已编排，依赖关系已标注。开始执行？"
```

#### 场景三：截图识别生成用例
```
测试员：[上传报错截图]
智能体："识别到登录页面错误提示：'系统繁忙，请稍后重试'

可能原因：
1. 后端服务超时
2. 数据库连接池耗尽

已生成验证用例4条，建议立即执行。需要我执行吗？"
```

#### 场景四：Git提交自动触发
```
开发者提交代码 → 智能体检测变更 → 分析影响范围 → 自动选择用例 → 自动执行 → 结果推送企业微信
```

#### 场景五：智能应答引导
```
用户："帮我测试"
智能体："好的，请问你想测试什么呢？接口、UI、APP还是其他类型？

如果不确定，我可以根据你最近的项目情况帮你推荐。"
```


## 3. 核心架构

```
┌─────────────────────────────────────────────────────────────────┐
│                     AI智能体测试平台                             │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                    对话交互层                              │ │
│  │  多轮对话 │ 智能应答 │ 主动交互 │ 打断处理 │ 多模态输入   │ │
│  └───────────────────────────┬───────────────────────────────┘ │
│                              │                                 │
│  ┌───────────────────────────▼───────────────────────────────┐ │
│  │                    认知决策层                              │ │
│  │  意图理解 │ 需求分析 │ 任务规划 │ 推理决策 │ 自我反思     │ │
│  └───────┬───────────┬───────────┬───────────┬───────────────┘ │
│          │           │           │           │                 │
│  ┌───────▼────┐ ┌────▼─────┐ ┌───▼────┐ ┌───▼────────┐        │
│  │  模型层    │ │ 提示词层 │ │ 知识库 │ │ Skills层   │        │
│  └───────┬────┘ └────┬─────┘ └───┬────┘ └───┬────────┘        │
│          │           │           │           │                 │
│  ┌───────▼───────────▼───────────▼───────────▼────────┐        │
│  │                     工具层                          │        │
│  │ HTTP │ Playwright │ Appium │ JMeter │ Locust │ adb │        │
│  │ 截图识别 │ 在线文档 │ Git │ 数据库 │ 代码执行        │        │
│  └───────────────────────┬───────────────────────────────┘        │
│                          │                                     │
│  ┌───────────────────────▼───────────────────────────────┐        │
│  │                  测试环境管理层                         │        │
│  │   dev/test/staging/prod │ 环境状态 │ 权限控制          │        │
│  └─────────────────────────────────────────────────────────┘        │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                 主动触发层                                │ │
│  │   Git Webhook │ 定时检查 │ 趋势监控 │ 异常告警            │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                   通知中心                                │ │
│  │   企业微信 │ 钉钉 │ 邮件 │ 平台内消息                     │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```


## 4. 功能模块总览

> 优先级表示目标产品的重要程度，不等同于发布批次。目标态P0能力可按依赖和资源安排在第二期交付；各期范围以本文件第23章路线图为准。

| 编号 | 模块               | 核心功能                                 | 优先级 |
| ---- | ------------------ | ---------------------------------------- | ------ |
| F1   | 对话交互系统       | 多轮对话、智能应答、打断处理、多模态输入 | P0     |
| F2   | 认知决策系统       | 意图理解、需求分析、任务规划、主动分析   | P0     |
| F3   | 任务管理系统       | 生命周期、排队、错误恢复                 | P0     |
| F4   | 测试能力系统       | 9大测试类型+需求分析全链路               | P0     |
| F5   | 测试环境管理       | 环境配置、权限、健康检查                 | P0     |
| F6   | 知识库系统         | 知识分类、审核沉淀、联动                 | P0     |
| F7   | Skills技能系统     | Skill定义、智能调度                      | P0     |
| F8   | 智能检索系统       | 混合检索                                 | P0     |
| F9   | 模型管理系统       | 多模型接入、路由切换                     | P0     |
| F10  | 提示词管理系统     | 四级层级、默认预置                       | P0     |
| F11  | 主动测试建议       | Git变更、趋势、异常通知                  | P1     |
| F12  | Git集成            | 仓库接入、变更分析、自动触发             | P0     |
| F13  | 通知中心           | 多渠道通知、防打扰                       | P0     |
| F14  | 报告系统           | 报告生成、评论协作、覆盖度               | P0     |
| F15  | 测试资产沉淀与复用 | 模板库、缺陷模式、快速启动               | P1     |
| F16  | 测试效果度量与ROI  | 时间节省、价值报告                       | P2     |
| F17  | 协作与权限系统     | 用户管理、审批流、操作级权限             | P0     |
| F18  | 安全与治理系统     | 加密、审计、脱敏                         | P0     |


## 5. F1 对话交互系统

### 5.1 多模态输入

| 编号   | 功能         | 说明                                 |
| ------ | ------------ | ------------------------------------ |
| F1.1.1 | 文本输入     | 自然语言测试需求描述                 |
| F1.1.2 | 文件上传     | PDF/Word/Excel/Markdown/APK/IPA/截图 |
| F1.1.3 | 图片输入     | 报错截图/UI设计稿/流程图/架构图      |
| F1.1.4 | 链接输入     | API地址/Swagger文档/在线文档链接     |
| F1.1.5 | 在线文档链接 | 腾讯文档/飞书/Notion/语雀/Confluence |
| F1.1.6 | 代码粘贴     | 代码片段/接口定义/JSON数据           |

### 5.2 多轮对话

| 编号   | 功能           | 说明                                    |
| ------ | -------------- | --------------------------------------- |
| F1.2.1 | 上下文记忆     | 记住整个会话内容，不丢失信息            |
| F1.2.2 | 指代消解       | 理解"这个""它""刚才的"等指代            |
| F1.2.3 | 话题切换与回溯 | 切换话题后可回到之前任务                |
| F1.2.4 | 澄清追问       | 信息不足时主动提问，一次最多3个关键问题 |
| F1.2.5 | 确认机制       | 关键操作前向用户确认                    |

### 5.3 打断处理（三模式）

| 打断类型       | 用户表达             | 智能体行为   | 结果处理         |
| -------------- | -------------------- | ------------ | ---------------- |
| 暂停（Pause）  | "等一下"、"暂停"     | 保留中间状态 | 补充信息后可继续 |
| 停止（Stop）   | "停止"、"不要执行了" | 停止当前执行 | 保留已完成结果   |
| 取消（Cancel） | "取消"、"算了"       | 取消当前执行 | 清理临时数据     |

**响应要求**：用户在对话中输入打断指令，智能体≤3秒响应，不等待当前任务完成。

### 5.4 主动交互

| 编号   | 功能     | 触发时机            | 说明             |
| ------ | -------- | ------------------- | ---------------- |
| F1.4.1 | 主动建议 | 发现测试遗漏/优化点 | 建议合理且不打扰 |
| F1.4.2 | 风险预警 | 检测到高风险操作    | 高风险必提醒     |
| F1.4.3 | 进度通知 | 长任务关键节点      | 25%/50%/75%/100% |
| F1.4.4 | 结果提醒 | 报告生成后          | 通知用户查看     |

### 5.5 进度通知模式

| 模式     | 说明                             | 默认 |
| -------- | -------------------------------- | ---- |
| 安静模式 | 仅在完成时通知                   | —    |
| 标准模式 | 关键节点通知（25%/50%/75%/100%） | ✅    |
| 详细模式 | 每条用例实时通知                 | —    |

### 5.6 智能应答与引导

| 编号   | 功能           | 说明                                                      |
| ------ | -------------- | --------------------------------------------------------- |
| F1.6.1 | 不合理输入识别 | 识别信息不足/目标模糊/逻辑矛盾/范围过大/信息冲突/不切实际 |
| F1.6.2 | 澄清追问       | 像人一样追问关键信息                                      |
| F1.6.3 | 建议式引导     | 用户不确定时主动分析推荐                                  |
| F1.6.4 | 风险确认       | 高风险操作前明确确认                                      |
| F1.6.5 | 友好拒绝       | 不合理/不合规请求友好拒绝+替代方案                        |
| F1.6.6 | 对话自然性     | 像同事交流，不机械不模板                                  |

**对话风格要求**：自然、简洁、有温度、专业、主动、不越界

**不合理输入处理示例**：
```
用户："帮我测试" → 智能体："好的，请问你想测试什么？接口、UI、APP还是其他？"
用户："测那个东西" → 智能体："抱歉，我没理解'那个东西'指什么，能具体说明吗？"
用户："测生产环境随便造数据" → 智能体："生产环境测试需谨慎，建议先在test环境验证。"
用户："1分钟测完1000个接口" → 智能体："1分钟完成1000个接口完整测试不太现实，连通性检查可以尝试。"
```


## 6. F2 认知决策系统

| 编号 | 功能         | 说明                                    | 验收标准                    |
| ---- | ------------ | --------------------------------------- | --------------------------- |
| F2.1 | 意图理解     | 识别用户测试意图                        | 准确率≥95%，支持10+意图类型 |
| F2.2 | 需求深度分析 | 功能拆解、关联识别、流程梳理、数据流转  | 功能拆解完整率≥90%          |
| F2.3 | 任务规划     | 拆解、排序、展示、确认                  | 拆解逻辑合理，覆盖完整      |
| F2.4 | 推理决策     | 可解释、可追溯                          | 每个决策可查看依据          |
| F2.5 | 自我反思     | 自检、调整、求助                        | 发现错误主动纠正            |
| F2.6 | 主动分析     | Git变更分析、质量趋势分析、环境异常分析 | 分析结果有依据              |
| 🟧 F2.7【V5.1】 | 🟧 受控多智能体协作 | 🟧 主编排器按复杂度选择单智能体或专业智能体团队；展示角色、分工、成本与决策依据 | 🟧 默认单智能体；启用多智能体前可解释、可确认、可中止 |


## 7. F3 任务管理系统

### 7.1 任务生命周期

```
创建 → 规划中 → 待确认 → 排队中 → 执行中 → 分析中 → 报告生成 → 已完成
  │       │        │        │        │        │          │        │
  └───────┴────────┴────────┴────────┴────────┴──────────┴────────┘
                         任意状态可打断（暂停/停止/取消）
```

### 7.2 任务类型

| 类型         | 说明                 | 触发方式     |
| ------------ | -------------------- | ------------ |
| 即时测试     | 一次性快速验证       | 用户对话发起 |
| 完整测试     | 系统化全流程         | 用户对话发起 |
| 定时回归     | 定时自动执行         | 用户设定时间 |
| Git触发      | 代码提交自动触发     | Git Webhook  |
| 主动建议触发 | 智能体主动建议后执行 | 用户确认建议 |

### 7.3 执行排队机制

| 场景         | 处理方式                   |
| ------------ | -------------------------- |
| 单人多个任务 | 按触发时间排队             |
| 多人同时触发 | 按用户优先级+时间排队      |
| 资源不足     | 提示排队位置和预计等待时间 |
| 紧急任务     | 测试负责人可提升优先级插队 |
| 用户取消排队 | 取消后不消耗资源           |

### 7.4 错误恢复机制

| 场景           | 恢复方式                  |
| -------------- | ------------------------- |
| 网络中断       | 自动重试3次，失败标记     |
| 环境不可用     | 暂停+通知+恢复后可继续    |
| LLM调用失败    | 自动降级到备用模型        |
| 代码生成失败   | 自动重新生成，标记原因    |
| Celery任务崩溃 | 自动重新入队，最大重试3次 |
| 数据库连接失败 | 自动重连，失败通知管理员  |


## 8. F4 测试能力系统

### 8.1 测试能力总览

| 编号 | 测试类型           | 技术栈                   | 优先级 |
| ---- | ------------------ | ------------------------ | ------ |
| F4.1 | 需求分析与用例生成 | LLM多轮确认              | P0     |
| F4.2 | 接口自动化测试     | pytest+requests+业务联调 | P0     |
| F4.3 | AI智能体测试       | 8维度评估                | P0     |
| F4.4 | UI自动化测试       | Playwright+POM           | P0     |
| F4.5 | 性能测试           | JMeter+Locust双引擎      | P0     |
| F4.6 | 安全测试           | 注入/越权/泄露           | P0     |
| F4.7 | 回归测试           | 智能选择用例             | P0     |
| F4.8 | APP测试            | Appium+adb               | P0     |
| F4.9 | 数据测试           | 一致性/完整性            | P1     |

### 8.2 F4.1 需求分析与用例生成（完整链路）

#### 完整流程图

```
需求输入（文件上传/在线链接/截图/手动描述）
    ↓
F4.1.1 文档解析（PDF/Word/Excel/Markdown/Swagger/在线文档/图片OCR）
    ↓
F4.1.2 需求深度分析（功能拆解/关联识别/流程梳理/数据流转）
    ↓
F4.1.3 联合功能需求识别（跨模块展示/依赖/状态联动/数据联动）
    ↓
F4.1.4 测试点分析（每个功能点的测试维度/边界/异常/联动）
    ↓
F4.1.5 用例生成（多轮对比确认：初始→对比→补全→修正→最终）
    ↓
F4.1.6 用例自动评审（多轮对比评审：初评→需求对比→修正→复审→最终）
    ↓
F4.1.7 自动化用例筛选（可自动化判断/难度/技术栈/优先级）
    ↓
F4.1.8 截图识别生成用例（页面元素/错误信息/功能点提取）
    ↓
F4.1.9 在线文档导入（腾讯/飞书/Notion/语雀/Confluence/Swagger/YApi）
    ↓
F4.1.10 需求版本与变更追踪（版本记录/变更检测/影响分析/更新建议）
    ↓
F4.1.11 测试数据准备与构造（数据需求/自动构造/依赖标注/准备清单）
    ↓
F4.1.12 用例执行顺序与依赖编排（依赖分析/顺序编排/前置检查/后置清理）
    ↓
F4.1.13 用例质量度量与持续优化（缺陷发现率/低质量识别/优化建议）
    ↓
F4.1.14 测试知识库联动（历史缺陷参考/类似用例参考/经验补充）
    ↓
F4.1.15 需求覆盖度量化报告（需求-测试点-用例映射/覆盖率/未覆盖标注）
```

#### F4.1.1 文档解析

| 功能       | 说明                                 |
| ---------- | ------------------------------------ |
| 本地文件   | PDF/Word/Excel/Markdown/TXT          |
| 在线文档   | 腾讯文档/飞书/Notion/语雀/Confluence |
| 接口文档   | Swagger/OpenAPI/YApi/Apifox          |
| 图片       | 截图/设计稿/流程图（OCR+视觉识别）   |
| 解析准确率 | ≥95%                                 |

#### F4.1.2 需求深度分析

| 编号     | 功能         | 说明                         |
| -------- | ------------ | ---------------------------- |
| F4.1.2.1 | 功能模块拆解 | LLM将需求拆解为独立功能模块  |
| F4.1.2.2 | 功能点提取   | 每个模块下提取具体功能点     |
| F4.1.2.3 | 功能关联识别 | 识别功能间调用/展示/数据关联 |
| F4.1.2.4 | 业务流程梳理 | 提取核心业务流程和操作路径   |
| F4.1.2.5 | 数据流转分析 | 分析数据在功能间的流转关系   |

#### F4.1.3 联合功能需求识别

| 编号     | 功能           | 说明                       | 示例                     |
| -------- | -------------- | -------------------------- | ------------------------ |
| F4.1.3.1 | 跨模块展示识别 | 识别"A功能在C页面展示"场景 | 订单详情页展示用户信息   |
| F4.1.3.2 | 功能依赖识别   | 识别功能间调用依赖         | 下单依赖登录和地址管理   |
| F4.1.3.3 | 状态联动识别   | 识别状态变化对其他功能影响 | 订单取消后库存回滚       |
| F4.1.3.4 | 数据联动识别   | 识别数据变更对其他功能影响 | 用户信息修改后订单同步   |
| F4.1.3.5 | 联动测试点生成 | 为每个联动场景生成测试点   | 测订单展示时验证用户信息 |

#### F4.1.4 测试点分析

每个功能点的测试维度：
- 正向场景
- 边界条件
- 异常场景
- 安全场景
- 联动场景
- 性能场景

#### F4.1.5 用例生成（5轮对比确认）

| 轮次  | 环节     | 说明                       |
| ----- | -------- | -------------------------- |
| 第1轮 | 初始生成 | LLM首次生成用例            |
| 第2轮 | 需求对比 | 重新读取需求，逐条对比覆盖 |
| 第3轮 | 遗漏补全 | 发现遗漏后自动补全         |
| 第4轮 | 偏差修正 | 发现偏差后自动修正         |
| 第5轮 | 最终确认 | 输出最终用例+覆盖度报告    |

#### F4.1.6 用例自动评审（5轮对比评审）

| 轮次  | 环节         | 说明                   |
| ----- | ------------ | ---------------------- |
| 第1轮 | 初步评审     | 覆盖度/合理性/去重检查 |
| 第2轮 | 需求对比评审 | 重新读取需求，逐条验证 |
| 第3轮 | 偏差修正     | 修正评审发现的问题     |
| 第4轮 | 复审         | 修正后再次评审         |
| 第5轮 | 最终评审报告 | 输出评审结论           |

#### F4.1.7 自动化用例筛选

| 功能           | 说明                           |
| -------------- | ------------------------------ |
| 可自动化判断   | 判断用例是否适合自动化         |
| 自动化难度评估 | 标注自动化实现难度（低/中/高） |
| 技术栈推荐     | 推荐合适的技术栈               |
| 自动化优先级   | P0优先/P1第二批/P2第三批       |

#### F4.1.8 截图识别生成用例

| 功能         | 说明                      |
| ------------ | ------------------------- |
| 页面元素识别 | 识别按钮/输入框/列表/导航 |
| 错误信息识别 | 识别错误提示/异常状态码   |
| 功能点提取   | 从截图中提取可测试功能点  |
| 交互逻辑分析 | 分析页面交互和操作路径    |
| 用例自动生成 | 基于识别结果生成用例      |
| 识别准确率   | ≥90%                      |

#### F4.1.9 在线文档导入

| 平台        | 接入方式         |
| ----------- | ---------------- |
| 腾讯文档    | 链接 + OAuth     |
| 飞书文档    | 链接 + API Token |
| 语雀        | 链接 + API       |
| Notion      | 链接 + API Token |
| Confluence  | 链接 + API       |
| Swagger UI  | 链接直接解析     |
| YApi/Apifox | 链接直接解析     |
| 通用网页    | URL抓取          |

#### F4.1.10 需求版本与变更追踪

| 功能         | 说明                        |
| ------------ | --------------------------- |
| 需求版本记录 | 记录每次上传/导入的需求版本 |
| 需求变更检测 | 检测在线文档更新            |
| 用例影响分析 | 需求变更后分析受影响用例    |
| 用例更新建议 | 给受影响用例提供更新建议    |
| 历史版本对比 | 对比不同版本需求差异        |

#### F4.1.11 测试数据准备与构造

| 功能         | 说明                         |
| ------------ | ---------------------------- |
| 数据需求分析 | 分析每条用例需要的数据       |
| 数据自动构造 | 生成边界值/特殊字符/大数据量 |
| 数据依赖标注 | 标注用例间数据依赖           |
| 数据复用建议 | 识别可复用的测试数据         |
| 数据准备清单 | 生成测试数据准备清单         |

#### F4.1.12 用例执行顺序与依赖编排

| 功能         | 说明                    |
| ------------ | ----------------------- |
| 执行依赖分析 | 识别用例间执行依赖      |
| 执行顺序编排 | 自动编排执行顺序        |
| 前置条件检查 | 执行前检查前置数据/状态 |
| 后置清理     | 执行后自动清理测试数据  |
| 失败跳过策略 | 前置失败时后续用例处理  |

#### F4.1.13 用例质量度量

| 功能           | 说明                       |
| -------------- | -------------------------- |
| 缺陷发现率统计 | 每条用例发现的缺陷数       |
| 低质量用例识别 | 长期未发现缺陷的用例       |
| 用例优化建议   | 基于执行结果的优化建议     |
| 高质量用例沉淀 | 高缺陷发现率用例标记为模板 |

#### F4.1.14 测试知识库联动

| 功能         | 说明                   |
| ------------ | ---------------------- |
| 历史缺陷参考 | 生成用例时检索历史缺陷 |
| 类似用例参考 | 检索相似模块历史用例   |
| 经验补充     | 基于历史经验补充测试点 |
| 高风险优先   | 历史高风险场景优先覆盖 |

#### F4.1.15 需求覆盖度报告

| 功能                 | 说明               |
| -------------------- | ------------------ |
| 需求-测试点-用例映射 | 三级映射追踪       |
| 覆盖率计算           | 量化需求覆盖率     |
| 未覆盖标注           | 标注未覆盖项和原因 |
| 可视化报告           | 覆盖度图表展示     |

### 8.3 F4.2 接口测试（含业务联调）

#### 执行模式

| 模式     | 场景           | 技术实现                     |
| -------- | -------------- | ---------------------------- |
| 即时模式 | 快速验证单接口 | 智能体直接HTTP请求           |
| 脚本模式 | 复杂断言       | Python + requests            |
| 完整模式 | 系统化测试     | pytest + requests + 数据驱动 |

#### 业务联调

| 编号   | 功能             | 说明                                    |
| ------ | ---------------- | --------------------------------------- |
| F4.2.1 | 单接口用例生成   | 基于接口文档生成单接口用例              |
| F4.2.2 | 业务链路识别     | LLM分析接口文档，识别接口间业务调用关系 |
| F4.2.3 | 业务联调用例生成 | 生成多接口串联的业务流程用例            |
| F4.2.4 | 联调数据流转     | 自动处理接口间数据传递（Token/ID/状态） |
| F4.2.5 | 联调断言         | 每个环节断言+最终结果断言               |

### 8.4 F4.3 AI智能体测试

#### 8个测试维度

| 维度       | 评分  |
| ---------- | ----- |
| 准确性     | 1-5分 |
| 相关性     | 1-5分 |
| 完整性     | 1-5分 |
| 上下文保持 | 1-5分 |
| 知识引用   | 1-5分 |
| 安全性     | 1-5分 |
| 格式合规   | 1-5分 |
| 稳定性     | 1-5分 |

**通过标准**：综合≥4.0且无关键维度<3分

### 8.5 F4.4 UI自动化测试

```
技术栈：Python + Playwright + pytest + POM + 数据驱动
覆盖：页面加载、元素交互、表单提交、页面跳转、响应式
截图：自动截图，失败必截图
```

### 8.6 F4.5 性能测试（双引擎）

| 引擎   | 适用场景              | 选择逻辑     |
| ------ | --------------------- | ------------ |
| JMeter | 已有JMX复用、复杂协议 | 已有脚本优先 |
| Locust | 新脚本、高并发        | 默认选择     |

**指标**：响应时间(P50/P90/P99)、吞吐量、错误率、瓶颈分析

### 8.7 F4.6 安全测试

SQL注入、XSS、命令注入、认证绕过、越权、敏感信息泄露、AI提示词注入

### 8.8 F4.7 回归测试

智能选择用例、失败重跑、定时回归、影响分析

### 8.9 F4.8 APP测试

| 能力      | 技术            |
| --------- | --------------- |
| 安装/启动 | Appium + adb    |
| 功能遍历  | Appium + pytest |
| 兼容性    | 设备矩阵        |
| 稳定性    | Monkey          |
| 性能指标  | 内存/CPU/帧率   |
| 网络场景  | 弱网模拟        |

### 8.10 用例优先级定义

| 优先级 | 定义                      | 处理要求   |
| ------ | ------------------------- | ---------- |
| P0     | 核心功能不可用/安全漏洞   | 立即修复   |
| P1     | 主要功能异常/影响多数用户 | 上线前修复 |
| P2     | 次要功能异常/有替代方案   | 可延迟     |
| P3     | 边缘问题/UI文案           | 可暂不修复 |


## 9. F5 测试环境管理

### 9.1 环境类型

| 环境    | 用途     | 默认权限   |
| ------- | -------- | ---------- |
| dev     | 开发自测 | 开发者可测 |
| test    | 标准测试 | 测试者可测 |
| staging | 预发布   | 负责人审批 |
| prod    | 生产     | 管理员审批 |

### 9.2 环境配置项

| 配置项       | 说明                            |
| ------------ | ------------------------------- |
| 环境名称     | dev/test/staging/prod           |
| 环境描述     | 用途说明                        |
| 接口基础地址 | 如 https://test-api.example.com |
| 数据库连接   | 测试数据库连接信息              |
| 认证信息     | 测试账号、Token、证书           |
| 环境变量     | 环境特定变量                    |
| 环境状态     | 可用/维护中/不可用              |
| 健康检查URL  | 健康检查接口                    |

### 9.3 健康检查

| 功能         | 说明            |
| ------------ | --------------- |
| 定时检查     | 每5分钟自动检查 |
| 手动检查     | 用户可触发      |
| 异常告警     | 不可用自动通知  |
| 自动恢复通知 | 恢复后通知      |

### 9.4 对话中指定环境

```
用户："帮我测登录接口"
智能体："请确认测试环境：
1. dev - 可用
2. test - 可用（推荐）
请选择？"
```


## 10. F6 知识库系统

### 10.1 知识分类

| 类别         | 内容                       | 来源          |
| ------------ | -------------------------- | ------------- |
| 内置知识     | 测试方法论、模板、安全基线 | 平台预置      |
| 项目知识     | 需求、用例、报告、缺陷     | 按项目隔离    |
| 经验知识     | 缺陷模式、策略、教训       | 跨项目共享    |
| 被测对象知识 | 被测AI/接口/系统           | 自动/手动录入 |
| 实时知识     | 进度、变更、环境状态       | 动态更新      |
| 个人知识     | 偏好、收藏                 | 用户私有      |

### 10.2 知识来源

| 来源     | 方式     |
| -------- | -------- |
| 平台内置 | 预置     |
| 手动录入 | 用户添加 |
| 文件上传 | 自动解析 |
| URL抓取  | 自动爬取 |
| API同步  | 自动拉取 |
| 对话沉淀 | 自动提取 |
| 测试沉淀 | 自动积累 |
| 批量导入 | CSV/JSON |

### 10.3 知识沉淀审核

```
自动提取 → 待审核 → 人工审核 → 入库/拒绝/修改/暂缓
```

| 审核规则     | 说明                 |
| ------------ | -------------------- |
| 敏感信息过滤 | 自动检测并过滤       |
| 冲突检测     | 与已有知识矛盾时标记 |
| 质量评分     | 低质量自动丢弃       |
| 来源标注     | 标注来源和可信度     |
| 审核时限     | 7天内审核            |

### 10.4 测试知识库联动

| 功能         | 说明                       |
| ------------ | -------------------------- |
| 历史缺陷参考 | 生成用例时检索相关历史缺陷 |
| 类似用例参考 | 检索相似模块历史用例       |
| 经验补充     | 基于历史经验补充测试点     |
| 高风险优先   | 历史高风险场景优先覆盖     |

### 10.5 LlamaIndex适配边界基线（阶段24实施）

- LlamaIndex用于文档连接、分块、索引和检索适配，不替代Django业务模型、审核状态或项目隔离。
- PostgreSQL 16与pgvector仍是平台自托管向量数据真理源；不得因接入框架强制使用外部托管云服务。
- 所有检索结果必须保留项目、来源、版本、审核状态和权限元数据，禁止跨项目召回。
- 阶段24必须允许降级到原生Django/pgvector检索实现，避免框架不可用时知识库整体失效。


## 11. F7 Skills技能系统

### 11.1 Skills清单

| 类别       | Skills                                                       | 优先级 |
| ---------- | ------------------------------------------------------------ | ------ |
| 核心测试   | 接口测试、AI测试、用例生成、报告生成、缺陷分析、需求分析     | P0     |
| 专项测试   | 安全测试、性能测试(双引擎)、UI测试、APP测试、数据测试、回归测试 | P0     |
| 辅助能力   | 数据构造、Mock、文档解析、截图识别、在线文档、日志分析       | P1     |
| 团队自定义 | 团队创建                                                     | P1     |
| 个人自定义 | 个人创建                                                     | P2     |

### 11.2 Skill定义结构

```yaml
skill:
  name: "接口安全测试"
  version: "1.0.0"
  description: "对REST API进行安全漏洞检测"
  category: "专项测试"
  triggers:
    explicit: ["安全测试", "漏洞扫描"]
    implicit:
      - context: "接口涉及认证"
        action: "自动附带安全检查"
    prohibited: ["用户明确说不要安全测试"]
  capabilities: ["SQL注入检测", "XSS检测", "越权检测"]
  tools: ["HTTP请求工具", "代码执行器"]
  input_schema: {}
  output_schema: {}
```

### 11.3 Skill智能调度

```
用户输入 → 意图识别 → 核心Skill必选 → 关联Skill推荐 → 用户确认 → 执行
```

| 调度依据   | 说明          |
| ---------- | ------------- |
| 用户意图   | 核心判断      |
| 上下文关联 | 自动推荐      |
| 历史偏好   | 用户习惯      |
| 风险评估   | 安全自动附带  |
| 用户指定   | 明确要求/禁止 |

### 🟧 11.4【V5.1 已人工验收】MCP能力接入

| 🟧 能力 | 🟧 产品要求 |
|---|---|
| 🟧 MCP Client | 🟧 连接经管理员批准的本地或远程MCP Server，展示来源、能力和健康状态 |
| 🟧 MCP Server | 🟧 按最小能力暴露平台Tools、Resources、Prompts，不暴露完整会话和跨项目数据 |
| 🟧 工具授权 | 🟧 只读低风险工具可按策略授权；写入、执行、外发数据等高风险操作必须HITL确认 |
| 🟧 可观测性 | 🟧 记录Server、工具、调用者、参数摘要、耗时、结果状态和审批记录，敏感值必须脱敏 |
| 🟧 失效处理 | 🟧 超时、断连、能力变化和协议不兼容时安全失败，不得静默改用高权限工具 |

### 11.5 Skills扩展与第三方兼容

平台必须同时支持内置 Skill、项目自定义 Skill 和经审核的第三方 Skill。自定义 Skill 不能只保存配置，必须能够绑定受控运行时实现，并经过输入输出契约校验后由智能体调用。

第三方 Skill 来源至少兼容本地 Skill 包、GitHub 仓库、SkillHub 地址和可下载的版本化包。所有来源必须记录来源地址、版本、提交哈希或文件哈希、作者、许可证、安装人、审核人和安装状态；禁止未审核代码直接执行。

第三方 Skill 安装必须经过管理员审批，支持版本锁定、启用/停用、卸载和回滚。Skill 必须声明网络、文件、数据库、外部命令、模型、知识库和环境凭据权限，默认拒绝高风险权限；运行时必须执行项目隔离、超时、资源限制、错误隔离和审计记录。

第三方来源不可用、内容校验失败、权限超出审批范围或运行时异常时，平台必须安全失败，并保留内置 Skill 和已批准本地 Skill 的正常使用能力。Skill 来源适配、安装和运行权限必须通过平台前端可见、可审计、可撤销。


## 12. F8 智能检索系统

### 12.1 检索方式

| 检索方式   | 说明            | 适用场景   |
| ---------- | --------------- | ---------- |
| 语义检索   | 向量相似度      | 模糊查询   |
| 关键词检索 | PostgreSQL FTS 排名 | 精确查询；P1再评估真正 BM25 |
| 混合检索   | pgvector + PostgreSQL FTS | 默认方式 |
| 元数据过滤 | 按项目/类型筛选 | 精确筛选   |
| 个性化检索 | 结合用户偏好    | 个人化结果 |
| 上下文感知 | 结合对话上下文  | 连续对话   |

### 12.2 检索配置

| 配置项     | 默认值  |
| ---------- | ------- |
| Top-K      | 5       |
| 相似度阈值 | 0.65    |
| 分块大小   | 500字符 |
| 重叠长度   | 50字符  |

P0 使用 PostgreSQL 全文检索（`tsvector`/`tsquery`/数据库排序）作为关键词召回，不把 FTS 排名无条件宣称为 BM25。真正 BM25 只有在单独验证数据库扩展、运维成本和排序收益后，才作为 P1 方案引入。

### 12.3 RAG 二阶段产品规划

#### 12.3.1 阶段定位与产品判断

当前知识库已经具备文档解析、分块、审核和基础检索能力，但还不能稳定支撑“基于企业知识回答并给出依据”的产品承诺。RAG 二阶段的目标不是增加一个框架名词，而是把知识库升级为可控、可解释、可评估的知识增强能力。

本阶段优先解决三个真实问题：

1. 用户问到接口名、错误码、字段名或版本号时，关键词检索不能丢失精确匹配；
2. 用户用自然语言描述问题时，语义检索能够找到同义或相近内容；
3. 智能体回答或生成测试用例时，用户能够看到引用来源，并确认答案没有跨项目或跨审核状态取数。

本阶段不做以下事情：

- 不把 LlamaIndex 作为业务数据库或权限系统；
- 不新增独立向量数据库，继续以 PostgreSQL + pgvector 为数据底座；
- 不把历史 JSON 向量直接当作生产检索结果，旧数据必须经过明确的重建或标记；
- 不承诺“有知识库就一定正确”，没有足够证据时必须回答“未找到可靠依据”；
- 不在本阶段同时引入 MCP、多智能体框架或新的主编排器。

#### 12.3.2 目标用户与核心场景

| 用户 | 场景 | 用户应看到的结果 |
| --- | --- | --- |
| 测试工程师 | 搜索某个错误码、接口字段或历史缺陷 | 结果按相关性排序，显示来源、项目、版本和审核状态 |
| 测试负责人 | 让智能体参考项目规范生成测试点或用例 | 生成内容带引用；引用不足时明确提示需要人工确认 |
| 开发工程师 | 查询接口契约、环境约束和已知问题 | 精确命中关键词，同时补充语义相近内容 |
| 管理员 | 选择知识模型、查看索引状态、处理失败重建 | 能看到模型、索引版本、失败原因、耗时和成本摘要 |

P0 首个闭环为：创建/上传知识 → 解析 → 人工审核 → 建立索引 → 混合检索 → 展示引用 → 智能体消费引用。

P1 才考虑个人化排序、自动知识沉淀、复杂查询改写和更多在线连接器；这些能力不能阻塞 P0。

#### 12.3.3 知识生命周期

```text
导入/上传
   ↓
解析与分块
   ↓
人工审核
   ↓
Embedding 生成
   ↓
pgvector + 关键词索引
   ↓
索引可用
   ↓
混合检索与引用
   ↓
文档变更/撤销 → 旧索引失效 → 增量或全量重建
```

只有同时满足以下条件的内容才能参与默认检索：知识库启用、文档解析成功、审核通过、索引状态可用、项目和用户权限匹配。审核撤回、文档删除、项目成员变更后，结果必须立即或在明确的索引刷新窗口内失效。

同一索引版本只能使用一种 Embedding 模型和固定维度。更换模型或维度时必须生成新的索引版本和对应存储，不能混查不同维度向量；新版本评测通过后才可原子切换为当前版本，旧版本保留至确认可回滚后再清理。历史 HashVectorizer 向量不得直接作为生产 RAG 结果。

#### 12.3.4 检索产品要求

1. 检索模式至少支持语义、关键词、混合三种模式，默认混合；用户能够看见当前模式。
2. 过滤条件至少包括项目、知识库、知识类别、来源类型、文档版本和审核状态；权限过滤由后端完成，前端参数不能绕过。
3. 混合检索必须先分别取候选，再去重和融合排序；不能简单把两个分数直接相加而不做归一化。
4. 对错误码、接口名、字段名、版本号等精确文本，关键词结果必须有机会进入最终 Top-K。
5. 每条结果返回标题、来源类型、文档位置或分块序号、项目、版本、审核状态、检索模式、分数和稳定引用 ID。
6. 没有可靠结果时返回空结果和下一步建议，不生成无来源的确定性答案。
7. 结果上下文必须有长度上限、重复片段去重和最大引用数量，避免把整篇文档发送给模型。
8. 可选重排序只能使用明确配置且能力匹配的模型；重排序失败时保留混合检索结果并标记失败原因。
9. 建立索引前必须执行敏感信息检测；包含 API Key、密码、Token、个人信息或明确禁止外发内容时，默认阻止外部 Embedding，或仅在完成脱敏并取得相应授权后发送。
10. 全量重建前必须显示预计文档数、分块数、模型调用量和费用区间；超过管理员设定的预算或并发上限时必须等待确认或自动停止。

#### 12.3.5 智能体增强要求

- 智能体只有在意图需要知识依据时才触发检索，不对简单 CRUD 或纯计算请求强行检索。
- `retrieve_node` 接收用户输入、项目上下文、知识库范围和权限上下文，返回统一的 `knowledge_context` 与引用列表。
- 生成答案时必须区分“检索到的事实”“模型推断”和“待用户确认”，不能把推断写成文档原文；每个关键结论必须能映射到一个或多个引用。
- 没有足够证据时，智能体必须返回“证据不足/未找到可靠依据”状态并请求澄清，不得为了保持对话流畅而补写看似合理的事实。
- 当检索服务不可用、模型未配置或引用不足时，智能体应说明限制并请求用户补充，而不是静默使用跨项目或未审核内容。
- RAG 结果不改变任务权限；检索到的文档不能自动获得执行工具、访问环境或外发数据的权限。

#### 12.3.6 用户界面与反馈要求

知识库工作台必须覆盖：

- 索引状态：未建立、排队中、处理中、可用、部分失败、失效；
- 空数据：没有知识库、没有审核通过内容、没有可用索引时分别说明原因；
- 成功：展示检索结果、引用和检索模式；
- 字段错误：明确指出项目、模型、Top-K、阈值或过滤条件的问题；
- 401/403：区分未登录和无权访问，不泄露资源是否存在的敏感信息；
- 服务失败：显示是解析、Embedding、索引、检索或重排序失败，并提供可安全重试的入口；
- 变更提示：修改文档或审核状态后提示索引可能需要重建。
- 全量索引：展示预计成本和影响范围，管理员确认后才执行；执行中可查看进度、取消任务，失败时保留上一版可用索引。
- 版本管理：管理员可查看当前/上一版索引，并在新版本失败或质量不达标时回滚到上一版；回滚后重新执行权限校验。
- 数据外发：使用外部 Embedding 前展示供应商、地域、发送内容类别和脱敏结果；未获授权时只能使用本地或阻止索引。

#### 12.3.7 RAG 二阶段验收指标

以下指标针对二阶段验收集，不替代全平台指标：

| 指标 | P0 目标 | 说明 |
| --- | --- | --- |
| 权限隔离 | 未授权召回 0 条 | 覆盖跨项目、归档知识库、未审核文档和撤销成员 |
| 引用完整率 | ≥98% | 有内容的最终回答必须能回溯到稳定引用 |
| 关键词命中率 | ≥95% | 错误码、接口名、字段名、版本号测试集 |
| 混合检索 Recall@5 | ≥90% | 人工标注的项目知识测试集 |
| 无依据拒答准确率 | ≥90% | 无相关知识时不编造来源 |
| 索引幂等性 | 100% | 同一文档重复索引不产生重复有效分块 |
| 索引失败可恢复率 | 100% | 失败可重试，不破坏上一版可用索引 |
| 交互响应 | 普通检索 P95 ≤2秒 | 不含外部模型冷启动和大规模重建 |
| 生产降级 | 可回到原生检索 | LlamaIndex 不可用时不影响已审核知识检索 |
| 版本切换 | 原子切换、可回滚 | 新索引失败不影响上一版服务 |
| 外部数据治理 | 未授权敏感内容外发 0 次 | 覆盖索引前检测、阻止、脱敏和审计 |
| 成本控制 | 超预算任务不自动执行 | 全量重建需预估、确认、限流和可取消 |

验收必须同时包含离线标注集、后端专项测试、全量回归、真实本地 API、工作台操作和跨项目权限验证；不能只用“接口返回 200”作为 RAG 质量证明。

#### 12.3.8 服务器端模型调用与部署网络门禁

连接测试、Embedding、需求分析、用例生成和智能体调用均由后端服务进程发起外部模型请求，浏览器能够访问互联网不能替代服务器端出网能力。目标部署环境必须在上线前明确以下条件：

- Django、Celery Worker 和需要实际调用模型的服务身份，均可解析供应商域名并建立 TCP 443/TLS 连接；
- 公司网络要求代理时，代理地址、认证方式、CA 证书和适用进程必须完成配置，后端与 Worker 不能只依赖开发者终端的代理；
- 供应商域名、地域地址、端口和网络出口策略纳入部署清单；禁止通过关闭整体安全策略来解决单个服务的出网问题；
- 连接测试必须在与线上相同的后端运行身份和网络路径下执行一次真实短调用；只用浏览器、管理员电脑或目录 HTTP 200 不能作为通过依据；
- 出网失败时，页面需区分 DNS、TCP/TLS、代理、供应商 HTTP、鉴权、额度和模型能力问题，并给出管理员可执行的处理建议；不得把所有失败都显示为“测试未通过”；
- 模型基础地址必须执行供应商域名/端口白名单、解析 IP 校验和私网地址拦截；禁止访问回环、RFC1918、链路本地、云元数据等地址，并防止 DNS rebinding；管理员权限不能替代目标地址安全校验；
- 自动化测试默认使用 Mock Provider；只有管理员或用户明确确认的连接测试、目标部署验收允许一次不携带业务数据的短调用，不自动重试，不以此替代单元和集成测试；
- 模型 Key、代理认证信息和完整上游地址中的敏感部分不得写入日志、审计详情或前端响应。

服务器端出网未通过时，模型连接测试及依赖真实模型的功能只能标记为“环境未就绪”，不能标记为产品能力已验收。

连接测试的错误分类、目标地址安全校验和前端可理解反馈由独立任务 T146 实现；T145 只负责目标部署环境的出网与代理验收。

### 12.3.9 模型连接配置审批与跨地域使用治理（二期）

第三方代理、自建境外 Gateway、境外服务器直连和跨地域模型执行不能仅作为模型配置中的一个 URL。它们会影响数据去向、密钥暴露范围、费用、供应商责任和项目权限，必须纳入独立的连接配置审批生命周期。

#### 连接配置状态

连接配置至少包含以下状态：

```text
草稿 → 待审核 → 审核通过 → 已验证 → 已启用
                  ├→ 已拒绝
                  ├→ 已暂停
                  └→ 已撤销
```

- 新建的第三方代理、自建 Gateway 或境外连接默认为“待审核”，不得直接用于生产调用；
- “审核通过”只表示治理和权限审批通过，不代表网络或模型调用成功；
- “已验证”必须由与线上相同身份的 Django/Celery Worker 完成安全连接测试；
- 只有同时满足“审核通过、已验证、已启用、当前用户和项目有权限”时，连接才可以用于业务调用；
- 连接过期、被撤销或被暂停后，所有引用它的模型必须停止调用或切换到明确批准的备用连接；
- 超时默认拒绝，不得自动放行。

#### 审核信息

审核申请必须记录：

- 连接类型：官方直连、第三方代理、自建 Gateway 或境外部署；
- 端点地址、部署地域、协议类型和支持供应商；
- 模型范围、能力范围和模型 ID 映射；
- Prompt、文件、测试数据和模型结果的数据去向；
- 第三方是否保存数据、密钥归属、预计费用和额度；
- 允许使用的租户、项目、环境和角色范围；
- 连接测试结果、失败阶段、测试时间和执行身份；
- 申请人、审核人、审核意见、有效期、撤销原因和变更记录。

#### 审批规则

- 普通用户不能创建、修改或启用任意代理和境外连接；
- 平台管理员负责连接创建、域名/IP 白名单和凭据管理；
- 涉及生产数据、敏感数据、跨境传输或预计费用超过阈值时，必须触发 Human-in-the-Loop 审批；
- 高风险连接原则上需要双人确认；
- 审批人不能审批自己创建且涉及自身项目的高风险连接；
- 审批通过后修改端点、协议、地域、凭据归属、数据范围或费用策略，必须重新审核；
- 连接健康失败不应自动改变审批状态，但必须阻止调用并通知管理员；
- 所有创建、提交、批准、拒绝、启用、停用、暂停、撤销、切换和密钥轮换操作必须写入审计链。

#### 业务调用前检查

模型调用前必须校验连接状态、项目/租户范围、用户权限、模型能力、数据策略和当前健康状态。备用连接只能在管理员或功能策略明确允许时使用，并在页面显示实际生效的连接和切换原因。

本节是二期连接架构的产品约束；在对应需求审核通过并拆入 `TASKS_V5.0.md` 前，不新增生产连接字段，不开放第三方代理或境外连接的业务入口。


## 13. F9 模型管理系统

### 13.1 支持模型

| 提供商    | 模型              |
| --------- | ----------------- |
| OpenAI    | GPT-4o/mini/GPT-4 |
| Anthropic | Claude全系        |
| Google    | Gemini            |
| 阿里      | 通义千问          |
| 百度      | 文心一言          |
| DeepSeek  | V3/R1             |
| 智谱      | ChatGLM           |
| 自定义    | OpenAI兼容        |
| Azure     | GPT系列           |
| 本地      | Ollama/vLLM       |

### 13.1.1 官方模型目录与模型选择

用户完成任意供应商选择，并填写有效的连接地址和凭据后，模型配置页必须提供该供应商当前可公开支持的模型名称/模型 ID 供选择，不能要求用户凭记忆手工填写官网模型名。

- 优先通过供应商官方模型目录 API 获取模型；供应商没有目录 API 时，使用经过维护、带来源和更新时间的官方目录快照，不允许抓取不稳定的第三方网页作为唯一来源；
- 目录项至少展示模型名称、模型 ID、能力类型（文本、视觉、Embedding 等）、协议/版本、来源、更新时间和可用状态；
- 必须区分“官网目录存在”“当前 Key/账号可访问”“当前地址可调用”三种状态。官网存在不等于当前账号有权限，也不等于当前部署环境已经连通；
- 按当前供应商、协议、模型能力、项目功能和已配置地址过滤选择项；需求分析不能选择仅视觉模型，截图分析不能选择纯文本模型，Embedding 不能选择聊天模型；
- 目录同步失败时保留最近一次已验证目录并明确标记过期，同时允许管理员输入自定义模型 ID；不得把过期目录或未经验证的模型标记为已可调用；
- 模型目录 API 不得把 API Key、代理凭据、完整敏感地址或原始响应返回前端；目录同步、失败原因、来源和更新时间必须可审计；
- OpenAI-compatible、自定义和本地 Provider 不得承诺“官网全部模型”。没有可验证官方目录时，页面必须明确显示“需手工填写/由当前端点提供”，而不是生成虚假模型列表；
- 目录刷新必须由后端发起，受供应商出网、SSRF、超时、频率限制和权限门禁约束，不得由浏览器直接跨域访问供应商。

### 13.2 模型接入管理

| 功能     | 说明                |
| -------- | ------------------- |
| 模型注册 | 添加新模型          |
| 模型测试 | 连通性验证          |
| 模型分组 | 按用途分组          |
| Key管理  | 多Key/轮换/过期提醒 |
| 用量统计 | token消耗/费用      |
| 限流配置 | QPS/TPM限制         |

### 13.3 路由策略

| 策略       | 说明               |
| ---------- | ------------------ |
| 默认模型   | 平台默认           |
| 按任务路由 | 不同任务不同模型   |
| 按成本路由 | 简单任务用便宜模型 |
| 按质量路由 | 复杂任务用强模型   |
| 故障切换   | 主模型失败切备用   |
| 用户指定   | 对话中指定         |

### 13.4 模型使用路由与功能选择整改计划（已审核，按任务执行）

当前模型配置仅覆盖模型注册、按类型默认和基础路由，尚未完整满足“平台全局使用或各功能独立选择模型”的产品要求。本节作为新增整改计划，审核通过前不计入已完成需求，也不改变现有任务完成统计；整改只新增或修正本节范围内的能力，不追溯性否定之前已完成并验收的功能，历史差异通过独立任务兼容处理。

#### 13.4.1 模型选择层级

所有实际调用大模型的功能必须提供模型选择入口，选择优先级统一为：

1. 当前功能本次操作临时选择的模型；
2. 当前功能已保存的模型绑定；
3. 平台全局默认模型；
4. 管理员明确启用的备用模型；
5. 用户主动选择的确定性基线。

纯 CRUD、文件上传、确定性计算等不调用大模型的功能不增加无意义的模型选择控件。

全局默认模型用于所有未单独配置的兼容功能，但不能强行用于不支持目标能力的任务。文本、视觉、向量、语音和媒体任务必须按模型能力过滤；没有兼容模型时应明确阻止执行并提示配置，不得静默调用错误模型。

#### 13.4.2 功能级模型入口

以下功能必须有独立的模型入口，并显示“继承全局默认”选项：

| 功能 | 选择范围 | 说明 |
| --- | --- | --- |
| 需求深度分析 | 全局默认 / 功能绑定 / 本次选择 | 需求拆解、功能模块、功能点和测试点 |
| 截图与视觉分析 | 全局默认 / 功能绑定 / 本次选择 | 仅展示视觉或全模态模型 |
| 测试用例生成 | 全局默认 / 功能绑定 / 本次选择 | 五轮递进生成 |
| 测试用例评审 | 全局默认 / 功能绑定 / 本次选择 | 五轮评审与覆盖检查 |
| 智能体执行 | 全局默认 / 智能体绑定 / 本次选择 | 智能体已有显式模型时优先使用 |
| 报告生成 | 全局默认 / 功能绑定 / 本次选择 | 生成报告和覆盖度说明 |
| 知识库模型任务 | 全局默认 / 功能绑定 | 向量化、重排序等按能力过滤 |

管理员可以在模型配置页设置全局默认模型和功能绑定；功能页面允许用户在本次操作中临时切换，并必须展示最终生效的供应商、模型、能力类型和选择来源。

#### 13.4.3 失败与备用策略

- 未配置模型：阻止模型操作，提示配置入口和所需能力。
- 模型能力不匹配：阻止调用，提示选择兼容模型。
- Key、权限、地域、额度、网络或供应商失败：显示具体阶段和模型信息，支持重试或切换。
- 备用模型：仅在管理员或功能策略明确允许时启用；切换必须在页面提示并写入审计。
- 确定性基线：只能由用户主动选择，不能用“模型评审降级”或“没有可用模型”模糊替代。

#### 13.4.4 五轮用例生成语义

五轮表示同一份需求的递进分析和覆盖补全，不是重复生成五份独立用例：

1. 主流程、验收标准和核心功能点；
2. 异常、权限、输入校验、依赖失败和安全风险；
3. 边界、状态、并发、重复提交、恢复和数据一致性；
4. 模块关系、数据流、接口契约和跨模块业务链路；
5. 测试点、风险和回归影响的最终遗漏检查。

每轮必须输入需求正文、深度分析结果、功能模块、功能点、测试点、联合场景和已有用例，只返回新增场景。结果必须保存本轮分析重点、本轮新增数、累计数、实际模型、切换状态和失败原因。

#### 13.4.5 可理解性与审计要求

页面禁止直接展示 `initial`、`requirement_compare` 等内部阶段码。每轮应显示中文阶段名称、本轮新增数量和累计数量。历史记录缺失累计值时由服务端兼容计算。所有模型调用必须记录模型配置、供应商、模型名称、选择来源、是否备用切换和失败原因，但不得记录 API Key 或原始敏感凭据。

本节对应的实施任务见 `TASKS_V5.0.md` 中“模型使用路由与功能选择整改计划（已审核，按任务执行）”。


## 14. F10 提示词管理系统

### 14.1 四级层级

```
优先级：即时指定 > 场景级 > 项目级 > 全局默认
```

### 14.2 默认提示词

平台预置场景默认提示词：

| 场景     | 内容                         |
| -------- | ---------------------------- |
| 全局默认 | AI测试智能体角色定义         |
| 需求分析 | 需求拆解/功能识别/测试点     |
| 用例生成 | 用例格式/优先级/多轮确认     |
| 用例评审 | 评审维度/对比规则            |
| 接口测试 | pytest规范/数据驱动/业务联调 |
| AI测试   | 8维度评估标准                |
| UI测试   | Playwright+POM规范           |
| APP测试  | Appium规范                   |
| 性能测试 | 双引擎选择策略               |
| 截图识别 | 视觉分析规范                 |
| 报告生成 | 报告结构/简洁清晰            |

### 14.3 提示词配置功能

| 功能     | 说明               |
| -------- | ------------------ |
| 默认预置 | 新用户无需配置     |
| 查看     | 查看当前生效提示词 |
| 编辑     | 修改后立即生效     |
| 版本     | 历史回滚           |
| 测试     | 测试提示词效果     |
| 模板库   | 一键应用           |
| 导入导出 | 团队分享           |

### 14.4 合并规则

```
合并顺序：全局 → 项目 → 场景 → 即时指定
身份定义类：高层覆盖低层
规则约束类：高层优先，低层补充
冲突处理：高优先级覆盖，冲突时提示
```


## 15. F11 主动测试建议

### 15.1 触发场景

| 场景     | 触发条件        | 行为              |
| -------- | --------------- | ----------------- |
| Git变更  | 代码提交        | 分析→建议回归     |
| 环境异常 | 健康检查失败    | 通知→建议暂停     |
| 质量下降 | 通过率下降>10%  | 分析→建议专项测试 |
| 性能劣化 | P99持续增加     | 建议性能测试      |
| 缺陷集中 | 模块缺陷>5个/周 | 建议深度测试      |
| 定期报告 | 每日/每周       | 质量摘要推送      |

### 15.2 通知模式

| 模式     | 说明          |
| -------- | ------------- |
| 静默     | 不主动打扰    |
| 关键通知 | 仅P0/异常     |
| 标准     | Git/趋势/定时 |
| 完整     | 所有建议      |


## 16. F12 Git集成与自动触发

### 16.1 仓库接入

| 功能     | 说明           |
| -------- | -------------- |
| 仓库配置 | 地址、认证     |
| Webhook  | 自动/手动配置  |
| 多仓库   | 一个项目多仓库 |
| 分支过滤 | 指定分支触发   |
| 文件过滤 | 指定文件触发   |

### 16.2 自动触发流程

```
Git提交 → Webhook → 变更分析 → 选择用例 → 选择环境 → 执行 → 分析 → 通知
```

### 16.3 触发规则

| 规则     | 说明         |
| -------- | ------------ |
| 提交触发 | 每次提交     |
| 合并触发 | PR/MR合并    |
| 定时触发 | 每天定时     |
| 手动触发 | 用户手动     |
| 条件触发 | 指定文件变更 |

### 16.4 与Jenkins关系

**平台具备独立Git自动触发能力，Jenkins是可选集成，非必须依赖。**


## 17. F13 通知中心

### 17.1 通知渠道

| 渠道       | 接入方式 | 优先级 |
| ---------- | -------- | ------ |
| 企业微信   | Webhook  | P0     |
| 钉钉       | Webhook  | P0     |
| 邮件       | SMTP     | P0     |
| 平台内消息 | 站内信   | P0     |
| 飞书       | Webhook  | P1     |

### 17.2 触发场景（12种）

| 场景            | 渠道           | 优先级 |
| --------------- | -------------- | ------ |
| 测试完成        | 企微+平台内    | P0     |
| 测试失败(P0/P1) | 企微+钉钉+邮件 | P0     |
| 测试启动        | 平台内         | P1     |
| 报告生成        | 企微+平台内    | P0     |
| 主动建议        | 平台内         | P1     |
| 环境异常        | 企微+钉钉      | P0     |
| Git自动触发结果 | 企微           | P0     |
| 排队通知        | 平台内         | P1     |
| 评论提醒        | 平台内+邮件    | P1     |
| 知识审核提醒    | 平台内         | P1     |
| 定时报告        | 邮件+企微      | P1     |
| 系统告警        | 钉钉+邮件      | P0     |

### 17.3 防打扰机制

| 机制       | 说明          |
| ---------- | ------------- |
| 智能汇总   | 同时段合并    |
| 优先级过滤 | 低优先级延迟  |
| 频率限制   | ≤20条/小时/人 |
| 静默时段   | 仅P0必达      |
| 通知去重   | 相同内容去重  |

### 17.4 通知发送逻辑

```
触发 → 级别判断 → 静默判断 → 偏好判断 → 频率判断 → 选择渠道 → 渲染 → 发送 → 日志 → 失败重试3次→降级平台内
```


## 18. F14 报告系统

### 18.1 报告类型

| 类型       | 用途         |
| ---------- | ------------ |
| 测试报告   | 正式交付     |
| 日报/周报  | 日常汇报     |
| 缺陷报告   | 开发修复     |
| 对比报告   | 版本评审     |
| 性能报告   | 性能分析     |
| APP报告    | APP发布      |
| 覆盖度报告 | 需求覆盖量化 |
| 汇报版     | 管理层       |

### 18.2 覆盖度报告

| 功能                 | 说明       |
| -------------------- | ---------- |
| 需求-测试点-用例映射 | 三级映射   |
| 覆盖率计算           | 量化覆盖率 |
| 未覆盖标注           | 标注原因   |
| 可视化图表           | 覆盖度展示 |

### 18.3 报告评论协作

评论、@提及、回复、状态标记、通知


## 19. F15 测试资产沉淀与复用

### 19.1 功能

| 编号  | 功能           | 说明                   |
| ----- | -------------- | ---------------------- |
| F15.1 | 用例集模板化   | 高质量用例集沉淀为模板 |
| F15.2 | 测试策略模板   | 不同项目类型策略模板   |
| F15.3 | 缺陷模式库     | 历史缺陷模式自动匹配   |
| F15.4 | 组织级资产库   | 跨项目统一管理         |
| F15.5 | 新项目快速启动 | 基于模板快速初始化     |


## 20. F16 测试效果度量与ROI

| 编号  | 功能         | 说明                 |
| ----- | ------------ | -------------------- |
| F16.1 | 时间节省度量 | 人工 vs 平台时间对比 |
| F16.2 | 缺陷发现效率 | 单位时间发现缺陷数   |
| F16.3 | 用例质量对比 | AI vs 人工用例对比   |
| F16.4 | 价值报告     | 平台价值量化         |
| F16.5 | ROI仪表盘    | 投入产出比展示       |


## 21. F17 协作与权限系统

### 21.1 角色权限矩阵

| 功能       | 管理员 | 测试负责人 | 测试工程师 | 开发者      | 访客 |
| ---------- | ------ | ---------- | ---------- | ----------- | ---- |
| 创建智能体 | ✅      | ✅          | ❌          | ❌           | ❌    |
| 创建用例   | ✅      | ✅          | ✅          | ❌           | ❌    |
| 执行测试   | ✅      | ✅          | ✅          | ✅(自己模块) | ❌    |
| 查看报告   | ✅      | ✅          | ✅          | ✅(自己模块) | ✅    |
| 管理用户   | ✅      | ❌          | ❌          | ❌           | ❌    |
| 配置环境   | ✅      | ✅          | ❌          | ❌           | ❌    |
| Git集成    | ✅      | ✅          | ❌          | ❌           | ❌    |
| 通知配置   | ✅      | ✅          | ✅(个人)    | ✅(个人)     | ❌    |
| 知识审核   | ✅      | ✅          | ❌          | ❌           | ❌    |

### 21.2 操作级权限与审批流

| 功能       | 说明                 |
| ---------- | -------------------- |
| 操作级权限 | 每个操作可单独授权   |
| 审批流     | 敏感操作需审批       |
| 双人确认   | 高风险操作需两人确认 |
| 临时授权   | 限时授权             |
| 操作审计   | 完整审计链           |

### 21.3 多人协作

用例共享、模板复用、报告评论、执行他人用例集、结果分享、冲突处理

### 🟧 21.4【V5.1 已人工验收】运行时Human-in-the-Loop

- 🟧 触发条件：生产环境操作、外部数据发送、代码执行、破坏性工具、权限提升、成本超过阈值及低置信度决策。
- 🟧 审批信息必须说明操作对象、影响范围、数据去向、预计成本、可撤销性和超时时间。
- 🟧 用户可批准、拒绝、修改参数或取消；超时默认拒绝，不得自动放行。
- 🟧 暂停后必须保存checkpoint并支持幂等恢复，恢复时重新校验用户、角色、项目和工具权限。
- 🟧 产品运行时HITL不替代本项目开发过程中的逐任务人工确认规则。

### 21.5 平台账号开通与同事授权闭环【P0 补漏】

管理员必须能够完成“开通同事账号→分配平台角色→加入项目→同事登录并使用”的完整流程，不能要求多人共用管理员账号，也不能只提供零散的后端接口。

#### 账号开通方式

- 管理员可以创建邀请或生成一次性开通链接；
- 被邀请人使用自己的账号标识和密码完成激活；
- 邀请链接必须具备有效期、一次性使用、撤销和审计能力；
- 如果系统支持管理员创建临时密码，首次登录必须强制修改，管理员不能查看或再次读取密码；
- 保留公开注册能力时，新注册用户默认进入待授权/访客状态，不能自动获得项目或平台管理权限；
- 管理员可以重新发送邀请、撤销未使用邀请、停用账号和发起密码重置；
- 不允许通过前端、日志或接口返回明文密码、邀请密钥或重置令牌。

#### 授权流程

平台角色和项目角色必须在产品上明确区分：

```text
账号开通
  → 平台角色（管理员/测试负责人/测试工程师/开发工程师/访客）
  → 项目成员关系（owner/manager/member/viewer）
  → 项目功能权限
  → 登录后可操作范围
```

- 管理员可以从用户列表进入授权流程；
- 管理员可以看到用户当前平台角色、账号状态、所属项目和项目角色；
- 管理员可以将已注册用户加入一个或多个项目，并设置项目角色；
- 需求导入、解析和深度分析至少要求项目 owner、manager 或平台管理员权限；
- 平台管理员权限不能绕过后端项目边界、目标地址安全校验或数据合规策略；
- 角色、项目成员、账号启停、邀请、重置和撤销均必须写入审计记录；
- 页面必须说明“平台角色变更”和“项目角色变更”不是同一项操作，避免管理员误以为只改一次角色即可授权全部功能。

#### 账号状态

账号至少支持：待邀请、待激活、已启用、已停用、已锁定、已撤销。停用、撤销或项目移除后，已签发的访问令牌必须按安全策略失效或在短时间内失效，不能继续访问原项目数据。

### 21.6 用户访问地址与部署入口【P0 补漏】

平台必须告诉管理员“应该把哪个地址给同事访问”，不能要求管理员在公网 IP、后端端口、Docker 容器 IP、数据库端口和 SSH 地址之间自行猜测。

#### 访问地址配置原则

- 生产环境必须显式配置一个规范的用户访问地址 `PUBLIC_APP_URL`，优先使用 HTTPS 域名；
- 没有域名时，可以使用公网 IP 加映射端口，例如 `http://公网IP:8090`，但必须明确这是临时或受限部署方式；
- API 地址默认使用同源路径；只有前后端分离部署时才额外配置 `PUBLIC_API_URL`；
- 内网地址、容器地址、数据库地址、Redis 地址和 SSH 地址不得作为普通用户访问地址展示；
- 系统不得自动枚举并向用户暴露服务器所有网卡 IP；
- 管理员可查看当前规范访问地址、协议、端口、访问范围（公网/内网）、TLS 状态和最近检查时间；
- 管理员可复制用户访问地址和邀请链接；
- 访问地址变更后，旧地址的处理方式、影响范围和生效时间必须可见。

#### 地址分类

| 地址类型 | 使用者 | 是否可分享 |
|---|---|---|
| 用户访问地址 | 同事、项目成员 | 可以 |
| 管理员入口地址 | 管理员 | 按部署策略 |
| API 内部地址 | Backend、前端代理 | 不直接分享 |
| Docker/数据库/Redis 地址 | 容器内部 | 不可分享 |
| SSH 管理地址 | 运维管理员 | 不可作为产品入口 |

#### 验收要求

- 管理员从工作台可以看到规范用户访问地址；
- 同事使用该地址可以完成邀请激活、登录和进入已授权项目；
- 访问地址错误、端口未开放、反向代理错误和 API 不可达时，页面给出可操作提示；
- 访问地址页面不得显示密钥、密码、内部服务地址或未经配置的推断 IP；
- 服务器部署验收必须记录实际用户入口，不得用 `127.0.0.1`、Docker 内网 IP 或仅后端 `8000` 端口替代公网入口。

### 21.7 长输入与长结构化结果分段生成闭环【P0 补漏】

需求分析只是长文本调用的一个入口。用例生成、五轮评审、报告生成和智能体结构化节点同样不能依赖一次无限增长的模型请求；固定提高 `max_tokens` 不能作为长内容成功的最终方案。

#### 统一运行规则

- 调用前必须根据供应商、模型上下文窗口、输入长度、结构化 Schema 和费用/超时策略计算预算；输入或预计输出超过单次预算时，自动生成可追踪的分段计划；
- 分段必须优先按文档章节、需求条目、用例集合、评审轮次或证据边界切分，不得简单按字符截断导致语义和引用丢失；每段必须携带项目、文档、需求/用例和证据 ID；
- 每次模型调用必须返回段编号、Schema 版本、续接游标、是否最终段和实际结束原因；`finish_reason=length` 只能进入受控续接/重分段流程，不能被当作成功 JSON；
- 合并必须是确定性的：按稳定 ID 去重、校验引用和必填字段、保留来源段和调用轨迹；重复提交或重试不得产生重复需求、用例、评审结论或报告内容；
- 某一段失败时，系统必须保留已完成段、显示部分结果状态和失败段，可单段重试或从游标继续；不得静默切换供应商、无限自动重试或把部分结果标记为完整/模型已验证；
- 页面和 REST 必须区分处理中、部分完成、已完成、可恢复失败和不可恢复失败，并给出当前段、失败原因、重试边界和费用提示；
- 该规则统一适用于需求分析、用例生成、五轮评审、报告生成、智能体结构化输出及后续新增的长结构化模型调用。

### 21.8 需求分析结果可重复性与覆盖完整性【P0 补漏，T155B】

需求深度分析属于模型辅助的结构化抽取，不能仅以“返回了合法 JSON”判定结果完整。相同需求、相同解析证据、相同模型路由和相同 Prompt 基线的重复分析必须具备可比较的指纹、证据覆盖状态和差异报告。

- 每次分析必须记录源文档指纹、解析证据指纹、实际模型路由、Prompt 配置版本/标识、Schema 版本、分段执行轨迹和完整性状态。
- 分析结果必须区分 `complete`、`partial`、`needs_review` 和 `failed`；分段失败、输出截断、证据未覆盖或结果数量显著下降时，不得展示为完整模型验证。
- 每个模块、功能点、联合场景和测试点应尽量关联来源证据；系统必须统计已覆盖、未覆盖和未提供引用的证据，不能用结构合法替代覆盖证明。
- 清除当前分析记录时，需求正文、解析证据和最近一次可比较分析基线必须按安全摘要保留，用于下一次分析的稳定性对比；历史用例生成记录不受影响。
- 重复分析的数量变化必须生成差异摘要。新结果低于上一版时，不得静默宣称两版等价，必须提示人工复核并保留当前结果状态。
- 只有完整性状态为 `complete` 的结果才可作为“已验证需求分析”进入后续用例生成；`partial` 和 `needs_review` 必须在工作台明确提示。
- `8192` 等单次输出上限只属于单段预算，超过预算必须通过可追踪分段、续接或重规划处理；合法但不完整的 JSON 不得被视为完整结果。

#### 验收要求

- 输入和输出均超过一次调用预算时，能够通过多个受控调用完成，并产生一份去重、字段完整、证据可追溯的最终结果；
- 任一中间段发生超时、服务失败、401/403、无效 JSON 或输出截断时，已完成内容不丢失，单段重试不会重复副作用；
- 空输入、字段错误、旧数据/旧接口、权限边界、供应商不支持、上下文边界和取消/恢复均有明确业务状态；
- 日志、调用轨迹和前端反馈不得泄露 Key、密码、完整敏感原文或代理凭据；未完成的部分结果不得进入“已验证”统计。


## 22. F18 安全与治理系统

| 安全项         | 实现          |
| -------------- | ------------- |
| API Key        | Fernet加密    |
| 密码           | Django哈希    |
| 数据传输       | HTTPS         |
| 数据脱敏       | 自动脱敏      |
| 权限控制       | RBAC          |
| 审计日志       | 全记录        |
| 文件上传       | 类型+大小限制 |
| Webhook Secret | 加密存储+验证 |

### 🟧 22.1【V5.1 已人工验收】MCP与多智能体安全基线

- 🟧 MCP Server默认不受信任，必须登记来源、版本、传输方式、授权范围和负责人。
- 🟧 防范工具描述投毒、间接提示词注入、同名工具替换、越权资源访问和跨Server数据泄漏。
- 🟧 工具参数使用结构化Schema校验；文件、网络、数据库和代码执行能力采用最小权限与隔离运行。
- 🟧 多智能体只共享完成职责所需的最小上下文；禁止默认广播凭据、完整会话或未脱敏业务数据。
- 🟧 外部框架故障不得绕过RBAC、HITL和审计链，降级路径必须保持相同安全级别。


## 23. 开发路线图

### 🟦 23.0【V5.2 已人工确认】纵向业务闭环交付

- 🟦 平台不再采用“全部后端完成后集中开发前端”的交付方式。每个可独立验收的业务模块必须依次完成后端、对应前端、真实API联调和用户验收。
- 🟦 决策者必须能从工作台导航找到该模块，并以其真实角色完成核心操作；仅能通过Shell、Postman或Django Admin操作，不视为产品功能验收完成。
- 🟦 每个页面必须设计加载中、空数据、成功、校验失败、服务失败和无权限状态；敏感信息不得因前端展示而扩大暴露。
- 🟦 用户验收关注业务名称、信息层级、操作路径、反馈语言和社会认知是否自然。发现理解偏差时在当前模块修正，不将交互债务推迟到项目末期。
- 🟦 数据库模型等不可独立操作的小任务可在同一模块内连续完成，但REST API完成后必须立即进入对应前端闭环。

### 第一期（MVP，10-12周）

| 周次    | 内容                                                  |
| ------- | ----------------------------------------------------- |
| 第1周   | 项目初始化、虚拟环境、用户认证、权限体系              |
| 第2周   | 🟦 模型管理前后端闭环、提示词管理前后端闭环           |
| 第3-4周 | LangGraph智能体、对话交互（含智能应答）、意图理解     |
| 第5周   | 🟦 测试环境管理前后端闭环                             |
| 第6-7周 | 需求分析全链路（解析→分析→测试点→用例生成→评审→筛选） |
| 第8周   | 接口测试（含业务联调）+ 执行引擎                      |
| 第9周   | AI测试 + 知识库 + 检索                                |
| 第10周  | 报告系统与覆盖度前后端闭环、全局交互回归              |
| 第11周  | 通知中心（企微+钉钉+邮件）                            |
| 第12周  | Git集成 + 部署上线                                    |

🟧 【V5.1 架构边界记录】第一期在知识库阶段预留LlamaIndex适配边界，在智能体阶段预留统一运行时接口；实际 RAG 实施转入阶段24，不在MVP同时启用多个主编排框架。

### 第二期（2-3个月）

- RAG 二阶段专项（见 12.3）：知识索引底座、真实 Embedding、LlamaIndex 适配、pgvector + 关键词混合检索、引用和智能体知识增强；建议作为本阶段最先启动的基础能力。
- 模型供应商出网与部署连通性硬门禁（见 12.3.8）：在服务器端完成 DNS、TCP/TLS、代理继承、供应商实际调用和失败诊断验证后，才进行依赖真实模型的生产验收。
- UI测试 + 性能双引擎 + APP测试
- 主动建议 + 多人协作 + 知识审核
- 测试资产沉淀与复用（F15）
- 操作级权限与审批流（F17.2）
- 需求版本与变更追踪（F4.1.10）
- 测试数据准备与构造（F4.1.11）
- 用例执行顺序与依赖编排（F4.1.12）
- 🟧 MCP能力网关、Server信任管理和高风险工具HITL

RAG 二阶段的交付顺序不是“先装 LlamaIndex，再把所有模块接上”，而是先完成检索契约和离线评测集，再完成知识库闭环，最后接入智能体。报告、主动建议和多智能体不得在 RAG 质量与权限验收前依赖其结果。

### 第三期（3-6个月）

- 用例质量度量与持续优化（F4.1.13）
- 测试效果度量与ROI（F16）
- Skill自定义 + CI/CD深度集成
- 多智能体协作 + 高级分析
- 🟧 CrewAI/AutoGen/Microsoft Agent Framework对比评估与单一可选运行时试点；AutoGen不作为默认生产核心


## 24. 验收标准

> 以下指标为V5.0目标态整体验收标准。第一期MVP按第23章第一期范围验收；UI、性能、APP等第二期能力不作为第一期上线阻断项。

🟦 【V5.2】“第二期UI”是指UI自动化测试能力，不代表产品操作界面延期；第一期内交付的每个业务模块都必须同时具备可验收的平台前端。

| 指标                 | 目标值                   |
| -------------------- | ------------------------ |
| 意图识别准确率       | ≥95%                     |
| 需求文档解析准确率   | ≥95%                     |
| 功能拆解完整率       | ≥90%                     |
| 联合功能识别率       | ≥85%                     |
| 用例生成覆盖率       | ≥95%                     |
| 用例评审通过率       | ≥90%                     |
| 自动化用例筛选准确率 | ≥85%                     |
| 截图识别准确率       | ≥90%                     |
| 在线文档支持平台     | ≥5个                     |
| 业务联调链路识别率   | ≥80%                     |
| 接口测试成功率       | ≥98%                     |
| AI测试覆盖维度       | 8个                      |
| 性能测试引擎         | JMeter+Locust            |
| APP测试平台          | Android/iOS/小程序       |
| 报告生成时间         | ≤30秒                    |
| 知识检索准确率       | ≥90%                     |
| 通知送达率           | ≥99%                     |
| Git自动触发延迟      | ≤1分钟                   |
| 环境健康检查         | 每5分钟                  |
| 系统可用性           | ≥99.9%                   |
| 支持模型             | ≥10类                    |
| 提示词层级           | 4级                      |
| 支持测试类型         | 9种                      |
| 多人同时使用         | ≥10人                    |
| 覆盖度报告           | 需求-测试点-用例三级映射 |
| 打断响应             | ≤3秒                     |
| 🟦 业务模块可视化闭环【V5.2】 | 🟦 已开放REST API的用户功能100%具备工作台入口和真实API联调 |
| 🟧 MCP未授权工具拦截率【V5.1】 | 🟧 100% |
| 🟧 HITL超时默认拒绝率【V5.1】 | 🟧 100% |
| 🟧 HITL暂停恢复一致性【V5.1】 | 🟧 关键状态与审批前一致，副作用不重复执行 |
| 🟧 跨项目检索隔离【V5.1】 | 🟧 未授权数据泄漏为0 |
| 🟧 多智能体启用原则【V5.1】 | 🟧 默认关闭；启用前展示分工、成本和风险并取得确认 |

### 24.1 RAG 二阶段专项验收补充

RAG 二阶段必须满足以下条件后，才可将“知识库模型任务”和“智能体知识增强”标记为完成：

1. 真实 PostgreSQL + pgvector 环境完成迁移、索引和回填验证；本地 SQLite 仅作为明确的降级测试环境。
2. 至少建立一套包含接口名、错误码、字段名、版本号、自然语言问题、跨项目干扰项和无答案问题的人工标注集。
3. 同一查询分别验证关键词、语义和混合检索结果，能够解释最终排序和引用来源。
4. 对未登录、跨项目、未审核、已归档和成员撤销场景完成 401/403/空结果/零泄漏验证。
5. 文档重新上传、重复索引、审核撤回、索引失败、重试和旧版本保留均有可重复测试。
6. LlamaIndex 适配关闭或异常时，原生 Django/pgvector 检索仍能安全返回；不能出现无提示的空上下文。
7. 智能体输出至少包含引用、证据不足提示和失败原因，不得把模型推断伪装成知识库原文；关键结论没有引用时不得标记为有依据。
8. 更换 Embedding 模型或维度时，新旧索引可并存、可评测、可原子切换和可回滚，不发生不同维度混查。
9. 向外部 Embedding 发送内容前完成敏感信息检测、脱敏/阻止、授权提示和审计；全量重建在预算确认前不能启动。
10. 目标部署环境必须由 Django 和 Celery Worker 的实际运行身份完成 DNS、TCP/TLS、代理继承和一次真实短调用验证；网络未就绪时不得标记模型能力验收通过。
11. 模型连接地址必须通过目标安全校验；连接测试页面必须展示 DNS、TCP/TLS、代理、供应商 HTTP、鉴权、额度和模型能力的脱敏失败阶段，并提供对应处理建议。
